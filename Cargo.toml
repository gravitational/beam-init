[workspace]
members = ["crates/docker_harness"]

[package]
name = "beam-init"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.185"
tokio = { version = "1.52.1", features = ["macros", "net", "rt"] }

[dev-dependencies]
docker_harness = { path = "crates/docker_harness" }
